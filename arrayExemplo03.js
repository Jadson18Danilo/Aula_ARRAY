const prompt = require("prompt-sync")()

let notas = [10, 5, 7 , 8, 9, 6, 8, 10 ,1]

// para adionar notas no array
// notas.push(10)
// notas.push(5)

// para alterar um valor do array
// notas[1] = 7

for (let i = 0; i < notas.length; i++) {
    console.log(`Nota ${i=1}: ${nota[i]}`)
}